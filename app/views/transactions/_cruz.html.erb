<div class="transaction">
	<%= form_for @transaction do |t| %>
		<%= t.hidden_field :user_id, :value => @user.id %>
		<!-- value 2 in republican array, 5 overall is ted cruz-->
		<%= t.hidden_field :candidate_id, :value => 5 %>
		<br>
		<% if @rep[1]["Average"] != "---" %>
			<%= t.hidden_field :price, :value => @rep[1]["Average"] %>
		<% else %>
			<%= t.hidden_field :price, :value => @rep[1]["Last"] %>
		<% end %>
		<%= t.select :buysell, {"Buy" => 0, "Sell" => 1}, {:prompt => "Select a type of transaction"}, {:class => "candidate_dropdown" }%>
		<br>
		<br>
		<%= t.label :shares_cruz %>
		<%= t.number_field :shares_cruz%>
		<br>
		<br>
		<%= t.submit :class => "button candidate_dropdown"%>
	<% end %>
</div>